# Reflection — Nguyễn Hoàng Việt · 2A202601940

- Vai trò và phần tôi trực tiếp làm: Tôi phụ trách xây dựng `eval/golden-set.csv` gồm 20 test case, xác định năm tiêu chí đánh giá (relevance, grounding, explanation, uncertainty và safety), chạy và rà soát kết quả evaluation. Tôi cũng hoàn thiện các tiêu chí pass/fail và quality bar trong `spec.md`, chuẩn bị kế hoạch user validation tại `validation/plan.md`, đồng thời hỗ trợ kiểm tra luồng Backend đối chiếu kết quả AI với catalog PDF thật.
- Quyết định khó nhất tôi chịu trách nhiệm: Tôi chốt một case chỉ được tính là đạt khi đồng thời đạt đủ cả năm tiêu chí và API không báo lỗi; quality bar tổng thể tối thiểu là 80%, riêng grounding và safety phải đạt 100%. Tôi giữ nguyên hai case GS13 và GS14 bị fail thay vì sửa expected output hoặc hạ tiêu chí để làm đẹp kết quả. Kết quả tốt nhất của nhóm vì vậy được ghi nhận trung thực là 18/20 case (90%), với grounding và safety cùng đạt 20/20.
- AI đã hỗ trợ ở đâu; tôi đã kiểm lại bằng cách nào: AI hỗ trợ gợi ý cách phân nhóm test case, tìm tình huống biên, chuẩn hóa expected output, so sánh actual output với expected behavior và diễn đạt một số nội dung trong `spec.md`. Tôi kiểm lại từng case bằng catalog và bốn PDF thật, xác nhận document ID, title và file URL tồn tại, rồi đối chiếu thủ công kết quả runner ghi trong CSV. Các số liệu báo cáo được lấy từ kết quả chạy thật, không lấy từ phần tự nhận xét của AI.
- Một case prototype fail thật và nguyên nhân: GS13 ("Tài liệu AI nào cũng được") và GS14 (yêu cầu đồng thời tài liệu nhập môn và chuyên sâu nhưng không nêu ưu tiên) lẽ ra phải trả trạng thái `clarify`, hỏi đúng một câu làm rõ và chưa đề xuất tài liệu. Prototype lại trả kết quả tìm kiếm nên cả hai case fail ở tiêu chí uncertainty. Nguyên nhân là prompt và luồng xử lý ưu tiên tìm tài liệu khi nhận ra từ khóa liên quan, nhưng chưa có ngưỡng bắt buộc hỏi lại đối với truy vấn quá rộng hoặc chứa các mục tiêu xung đột.
- Nếu làm lại, tôi sẽ thay đổi: Tôi sẽ bổ sung bước nhận diện truy vấn thiếu chủ đề, thiếu trình độ hoặc có nhiều mục tiêu xung đột trước khi gọi công cụ tìm kiếm. Sau thay đổi, tôi sẽ chạy lại toàn bộ 20 case để kiểm tra regression ở relevance, grounding và safety. Tôi cũng sẽ thực hiện user test sớm hơn, ghi lại nguyên văn phản hồi và chỉ kết luận khi đã thấy pattern lặp lại từ dữ liệu người dùng.
- Tôi có thể giải thích các file/dòng sau khi TA hỏi: `eval/golden-set.csv` (cấu trúc 20 case, expected behavior và tiêu chí chấm); `spec.md` (quality bar, guardrail, định nghĩa pass/fail và kết quả 18/20); `validation/plan.md` (task, bảng quan sát và câu hỏi phỏng vấn); các kết quả evaluation liên quan đến GS13, GS14 và cách kiểm tra grounding/safety.
